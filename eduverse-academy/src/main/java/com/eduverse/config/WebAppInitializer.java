package com.eduverse.config;

// Intentionally empty — dispatcher servlet registration is handled by web.xml.
// Having both web.xml and AbstractAnnotationConfigDispatcherServletInitializer
// causes a "Failed to register servlet with name 'dispatcher'" error.
